# ローカル開発環境用の設定ファイル
# このファイルを .env.local にコピーしてローカル開発に使用してください
# .env.local は .gitignore に含まれているため、Git にコミットされません

# 開発モード設定（ローカル開発専用）
# LINE認証とSupabaseをスキップして、インメモリストレージでテストできます
DEV_MODE_SKIP_AUTH=true
DEV_MODE_TEST_USER_LINE_SUB=dev_test_user_12345

# ローカル開発用のアプリケーションURL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ローカル開発用のCORS設定
ALLOWED_ORIGINS=http://localhost:3000

# セッション設定（ローカル開発用）
SESSION_SECRET=local_dev_secret_change_this_in_production
SESSION_EXPIRY_DAYS=7

# ========================================
# 以下の設定は開発モード(DEV_MODE_SKIP_AUTH=true)では不要です
# 実際のLINE認証やSupabaseを使用する場合のみ設定してください
# ========================================

# Supabase Configuration（開発モードでは不要）
# SUPABASE_URL=your_supabase_project_url
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# AWS Configuration（開発モードでは不要、実際のEC2操作をテストする場合のみ）
# AWS_REGION=ap-northeast-1
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_EC2_INSTANCE_ID=your_ec2_instance_id

# Minecraft Server Configuration（開発モードでは不要）
# MINECRAFT_SERVER_HOST=localhost
# MINECRAFT_SERVER_PORT=19132
# MINECRAFT_HEALTH_CHECK_URL=http://localhost:8080/health
# MINECRAFT_STARTUP_DELAY_SECONDS=180

# Rate Limiting（ローカル開発では緩めに設定）
RATE_LIMIT_AUTH_MAX_REQUESTS=100
RATE_LIMIT_AUTH_WINDOW_SECONDS=60
RATE_LIMIT_API_MAX_REQUESTS=1000
RATE_LIMIT_API_WINDOW_SECONDS=60
